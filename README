To add things to git repository:

git add example.txt
git commit -m "some note on what you changed"
git push origin master


There may be some quicker way to do this but I can't find it and this works.